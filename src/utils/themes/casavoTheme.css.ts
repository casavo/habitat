import { createTheme } from "@vanilla-extract/css";
import { defaultThemeValues, themeContract } from "./themeContract.css";

export const CasavoHabitatTheme = createTheme(
  themeContract,
  defaultThemeValues
);
